# 测试文档（Testing）

版本：v2.21.0 | 更新：2026-08-13

## 1. 测试策略

- **数据层自动化**：计算/存储/内容完整性用 node 单测，`node test.js` 一键运行，数据层断言 + 页面冒烟（共 401 项）
- **页面层冒烟**：mock `Page`/`wx` 实跑页面交互逻辑（编辑/保存流程），见第 4 章
- **安全专项测试**：6 套专项验证脚本覆盖安全/边界/压力/注入/XSS/并发场景（共 315 项 + 26 项工具函数测试）
- **手工**：页面渲染与视觉在微信开发者工具中验证（见第 5 章）
- **回归**：任何数据层改动后必须 `node test.js` 全绿；页面逻辑改动后跑冒烟测试再提交

## 2. 自动化测试环境

test.js 通过 mock 全局 `wx` 对象（内存 Map 实现 get/setStorageSync），使 utils 层脱离小程序运行：

```js
global.wx = {
  _store: {},
  getStorageSync(key) { return this._store[key]; },
  setStorageSync(key, val) { this._store[key] = val; }
};
```

## 3. 数据层测试用例清单（125 项）

### 3.1 动作库完整性（17 项）

| # | 断言 | 验证点 |
|---|------|-------|
| 1 | 共 173 个动作 | 动作总量（v2.2 起） |
| 2 | 10 个部位 | 部位总量 |
| 3 | 动作 id 无重复 | 数据一致性 |
| 4 | 全部动作字段完整 | steps≥2 / errors≥2 / target≥1 / rest / tip / difficulty∈1-3 / equipment |
| 5 | 每个部位都有动作 | 无空部位 |
| 6 | 部位知识完整 | freq / tips≥3 / recommended≥3 |
| 7 | 10 个部位都有关联知识文章 | articleIds 非空 |
| 8 | 胸部关联训练原理文章 | volume-intensity 在映射中 |
| 9 | 关联文章 id 真实存在 | 知识库可查到 |
| 10 | 新扩充动作已入库 | landmine-press / nordic-curl / swimming / handstand-pushup |
| 11 | 新动作字段完整 | calf-press-single target 非空 |
| 12 | muscleName 兼容别名 | stats 页调用可用 |
| 13 | getExercise 查询 | 按 id 检索 |
| 14 | 难度文案 | 1/2/3 → 入门/进阶/高级 |
| 15 | 类型文案 | compound/isolate → 复合/孤立 |
| 16 | 器械文案 | barbell → 杠铃 |
| 17 | 搜索"卧推" | ≥3 个命中且名称包含 |

### 3.2 知识库完整性（6 项）

| # | 断言 | 验证点 |
|---|------|-------|
| 12 | 共 30 篇文章 | 文章总量 |
| 13 | 5 个分类 | 分类总量 |
| 14 | 文章 id 无重复 | 数据一致性 |
| 15 | 全部章节结构有效 | para 有 content / list 有 items |
| 16 | 术语表 4+ 节 | 专项抽查 |
| 17 | 分类名映射 | categoryName('plans')='分化计划' |

### 3.3 容量计算（3 项）

| # | 断言 | 验证点 |
|---|------|-------|
| 18 | w1 容量 2560 | 60×10+70×8+80×5+100×5+100×5 |
| 19 | w1 组/次/最大重量 | 5 组 / 33 次 / 100kg |
| 20 | 自重动作容量 | 0×12+5×8=40（附加重量计入） |

### 3.4 存储层（3 项）

| # | 断言 | 验证点 |
|---|------|-------|
| 21 | 保存 3 条 | CRUD 写入 |
| 22 | 按时间倒序 | 排序正确 |
| 23 | 删除后剩 2 条 | 删除幂等 |

### 3.5 统计计算（15 项）

| # | 断言 | 验证点 |
|---|------|-------|
| 24 | 本周容量 2600 | 周窗口聚合 |
| 25 | 上周 500 / +420% | 环比计算 |
| 26 | 胸部容量 2060 | 按部位聚合（含跨周） |
| 27 | 背 40 / 腿 1000 | 部位聚合多值 |
| 28 | 近 8 周 / 本周柱 2600 | 趋势序列 |
| 29 | 上周柱 500 | 周边界正确 |
| 30 | 热力图 4 周 × 7 天 | 日历结构 |
| 31 | 热力图 w3 当日容量 500 | 按天聚合 |
| 32 | 热力图本周一容量 2560 | 当天容量正确 |
| 33 | 热力图最大日容量 | maxVol ≥ 2560 |
| 34 | 最大容量日 level 4 | 四档分档上限 |
| 35 | 无训练日 level 0 | 空档位 |
| 36 | 默认 12 周 | 周数默认值 |
| 37 | 空数据热力图安全 | 边界（maxVol=1） |
| 38 | 卧推 PR 80kg | 最大重量 |
| 39 | 卧推最佳单组 600 | 60×10 为最大单组 |

### 3.6 工具函数（2 项）

| # | 断言 | 验证点 |
|---|------|-------|
| 32 | 时长格式化 | 55→"55分钟"，80→"1小时20分" |

### 3.7 计划库（方向一，13 项）

- 计划数 5 套 / 训练日 17 个（v2.2 起）
- 计划 id 无重复、每套计划 days 非空
- getPlan / getPlanDay 查询、无效 id 返回 null
- buildDraftFromPlan：组数预填、力竭动作（reps=null）留空自填、动作不存在跳过、note 透传
- planSummaries：exerciseCount 汇总正确

### 3.8 记录体验（方向二，4 项）

- 热身组（warmup:true）不计入容量/组数/次数，但计入 warmupSets
- frequencyByExercise 计数正确（含热身组出现）
- sortByFrequency 常用动作置顶、未用过的保持原顺序
- RPE 字段随记录保存

### 3.9 数据分析（方向三，9 项）

- Epley 1RM：常规值 / reps>20 返回 0 / 空输入安全
- est1RMHistory：时间正序、每日取最大估算、热身组排除
- 体重：bodyweightTrend 最新/变化量/极值、空列表安全、addBodyweight 追加

### 3.10 内容扩充（方向四，12 项）

- 动作库总量 173、新增动作字段完整
- 新计划（减脂 4 日 / 居家 3 日）可解析
- 营养计算器：BMR/TDEE/蛋白质区间/增肌减脂热量、男女性差异、非法性别/活动水平/年龄拦截

### 3.11 工程健壮性（方向五，26 项）

- schema 迁移：全新安装 v3 / workouts 与 bodyweight 与 custom_plans 初始化 / v1 遗留补 key / v1→v3 / v2 老数据补 custom_plans / v2→v3 保留数据
- 导出结构完整（app/版本/数据/customPlans）
- 导入：非法项过滤、数据生效、非法计划过滤、v2 老备份兼容、非本应用数据拦截、结构错误拦截、null 拦截
- 自建计划 CRUD：保存/读取/删除/genPlanId 前缀
- 清空全部数据
- dataSizeBytes / formatSize 容量估算与格式化

### 3.12 数据分析增强（v2.3，8 项）

- est1RMTrend：2 点序列/最新值/高度归一化/无记录为空/单点最小高度
- planDayStatus：已打卡/其他日未打卡/无 plan 标记不计入
- planDayCompletion：2/5=40%/无训练 0%
- 计划合并：getPlan 命中自定义/不传查不到/自定义填充草稿/组数预填/汇总含自定义/自定义标记

### 3.13 自建计划页冒烟（v2.3，22 项）

- 新建默认 1 训练日、动作列表加载
- 添加训练日 → 2 个、自动切换
- 添加动作/重复拦截/默认 3×10/第二个动作
- 组数/次数编辑、训练日改名
- 保存入库：名称/custom 标记/结构/组次/空次数存 null
- 编辑态加载、删除训练日
- 校验拦截：无动作/无名称
- 删除计划、清空恢复

## 4. 页面冒烟测试（32 项，v2.2 动作详情 10 + v2.3 自建计划 22）

mock `Page`/`wx` 环境实跑页面交互逻辑（test.js 第 10/11 节）：

```js
// 关键 mock：Page 捕获配置 + setData 支持路径 key（'editing.sets[0].weight'）
global.Page = cfg => { pageCfg = cfg; };
global.wx = { getStorageSync, setStorageSync, showToast, showModal: o => o.success && o.success({confirm:true}), ... };
const page = Object.create(pageCfg);
page.setData = function (obj) { /* 路径解析后写入 this.data */ };
```

覆盖断言（详情页）：

1. 动作详情加载（bench → 杠铃卧推）
2. 部位训练知识加载（胸部训练知识）
3. 部位要点 ≥3 条
4. 同部位推荐动作 ≥3 个
5. 关联阅读 2 篇（训练原理）
6. 关联文章标题/摘要/分类完整
7. 推荐动作跳转（exercise-detail?id=xx）
8. 关联文章跳转（knowledge-detail?id=xx）
9. 其他部位动作关联知识（深蹲 → 腿部）
10. 不存在的动作显示空态

> 训练页编辑/保存全流程冒烟（不可变更新、空数据过滤，BUG-003/004 回归）为文档方法（上文 mock 片段），当前以 doc/ 第 5 节手工清单 + 开发者工具实测为准。

数据层 + 页面层（动作详情 / 部位训练页 / 计算器 / 统计热量 / 食物页 / 训练保护 / 训练页 v2.8 新功能 / 自建计划 / E2E 全链路 / secondary 词汇守门 / lastRecord 纯函数 / 边界守门 / 安全守门 / 发力图守门 / 工具函数）= 401 项全绿为提交门槛。专项验证脚本见第 8 章。

## 5. 手工测试清单（开发者工具）

| 模块 | 用例 |
|-----|------|
| 训练页 | 选部位→选动作→填组→完成动作→继续添加→保存；加一组/删一组/热身标记即时刷新；输入数字不丢光标；空组过滤；至少保留一组；计时递增；备注保存；计划填充（计划库→开始训练→草稿预填） |
| 训练页 v2.8 | 休息计时 30/60/90/120s + 自定义秒数（非法值拦截）；休息期间训练计时自动暂停、结束自动恢复；最后 3 秒数字高亮；动作卡"上次 8月11日 · 60kg×8"标签 + 添加预填 + 组编辑器"已带入"提示条与清空预填；计时暂停/继续（暂停不计时长、显示累计暂停分钟）；已添加动作上移/下移排序（首尾拦截、保存顺序一致）；训练页→动作库入口（自动筛部位）+ 返回条回训练 |
| 计划库 | 计划切换；日清单动作/组次展示；力竭动作"自填"标记；完成徽标与进度条；开始训练跳转填充；自建计划新建/编辑/删除 |
| 自建计划 | 新建默认训练日；添加/删除训练日与动作；重复动作拦截；组数次数修改；空次数=自填；计划名必填校验；保存后二次编辑不重复 |
| 动作库 | 部位切换；类型/难度筛选；搜索"深蹲"；点击进详情 |
| 动作详情 | 信息条/步骤/错误/贴士渲染；部位训练知识/同部位推荐/关联阅读渲染；推荐动作与关联文章跳转；"去记录"跳回训练页且预选动作 |
| 知识库 | 分类切换；文章列表；详情渲染段落与列表 |
| 历史页 | 展开/收起；时长与备注显示；删除二次确认；分享卡片生成与保存（含权限拒绝引导）；分享按钮（右上角胶囊） |
| 数据管理 | 导出复制剪贴板；从剪贴板导入（非法 JSON 提示/覆盖确认/含自建计划提示）；清空二次确认；容量展示 |
| 统计页 | 无数据显示空态；有数据后频率/容量/柱状图/分布/PR 渲染；PR 1RM 迷你趋势柱；训练热力图 12 周渲染与滚动；新保存训练后数据刷新；本周简报数字正确；体重记录（非法输入拦截/趋势图渲染） |
| 边界 | 全部输入为空时保存提示；重量/次数输字母（type=digit/number 限制）；切后台再回前台计时续走；动作不填组直接保存（BUG-004 回归） |

## 6. 回归流程

1. `node test.js` 全绿（401 项）
2. 页面冒烟测试全绿（动作详情 10 项 + 自建计划 22 项）
3. `node scripts/verify-page-match.js` + `node scripts/verify-nav.js` 全绿
4. `node scripts/verify-boundaries.js` 全绿（边界矩阵 45 项）
5. `node scripts/verify-extreme.js` 全绿（极限/压力/安全威胁 64 项）
6. `node scripts/verify-hardening.js` 全绿（高强度安全/容量/注入 61 项）
7. `node scripts/verify-security-round2.js` 全绿（XSS/状态/完整性/并发/内存 73 项）
8. `node scripts/verify-security-final.js` 全绿（综合安全测试 147 项）
9. `node scripts/verify-user-scenarios.js` 全绿（用户场景测试 68 项）
10. 开发者工具编译无报错
11. 手工清单按改动模块抽查
12. 提审前完整跑一遍手工清单 + 手机真机预览

## 7. 用户场景测试（verify-user-scenarios.js）

### 7.1 新手用户错误操作（11 项）

| 场景 | 测试内容 | 预期结果 |
|------|----------|----------|
| 空训练保存 | draft为空时点击保存 | 拦截，提示"还没有记录任何动作" |
| 全空组保存 | 只有空组（重量/次数都为空） | 拦截，提示"没有可保存的有效数据" |
| 重量输字母 | weight='abc' | 转换为0保存 |
| 负数次数 | reps='-5' | 转换为0保存 |
| 超大数值 | weight='99999', reps='999' | 正常保存不崩溃 |
| 小数重量 | weight='62.5' | 正确保存为62.5 |
| 营养计算器非法输入 | 年龄/身高/体重为字母或0 | 拦截，显示错误提示 |
| 食物热量负数克数 | 输入-100g | 拒绝输入，保持原值 |
| 食物热量字母克数 | 输入'abc' | 拒绝输入，保持原值 |

### 7.2 快速连续点击（4 项）

| 场景 | 测试内容 | 预期结果 |
|------|----------|----------|
| 快速添加同一动作 | 10次快速点击同一动作 | 只保留1个（进入编辑模式） |
| 快速保存 | 5次快速点击保存 | 不崩溃，数据一致 |
| 快速切换休息计时器 | 快速启动/停止/启动/停止 | 不崩溃，状态正确 |
| 快速暂停/继续 | 10次快速切换暂停状态 | 不崩溃，状态正确 |

### 7.3 页面切换中断（3 项）

| 场景 | 测试内容 | 预期结果 |
|------|----------|----------|
| 训练中切换页面 | 有草稿时onHide | 保持训练状态 |
| 编辑中切换页面 | step='edit'时onHide | 保持编辑状态 |
| Tabata运行中切换 | tabataRunning=true时onHide | 保持运行状态 |

### 7.4 数据损坏恢复（8 项）

| 场景 | 测试内容 | 预期结果 |
|------|----------|----------|
| workouts存储损坏 | 存储值为字符串'corrupted' | 返回空数组 |
| bodyweight存储损坏 | 存储值为对象{bad:true} | 返回空数组 |
| customPlans存储损坏 | 存储值为数字12345 | 返回空数组 |
| intake存储损坏 | 存储值为null | 返回空数组 |
| 版本字符串损坏 | schema='abc' | 保留数据，补结构 |
| 导入损坏数据 | 含null/非法ts/非法items | 过滤非法项，导入有效项 |

### 7.5 边界值输入（12 项）

| 场景 | 测试内容 | 预期结果 |
|------|----------|----------|
| 体重20kg | 最小有效体重 | 保存成功 |
| 体重300kg | 最大有效体重 | 保存成功 |
| 体重0kg | 无效体重 | 拒绝保存 |
| 体重负数 | 无效体重 | 拒绝保存 |
| 体重NaN | 无效体重 | 拒绝保存 |
| 体重Infinity | 无效体重 | 拒绝保存 |
| 体重501kg | 超出范围 | 拒绝保存 |
| 营养计算器最小值 | 10岁/100cm/30kg/活动1 | 计算成功 |
| 营养计算器最大值 | 100岁/250cm/300kg/活动5 | 计算成功 |
| 营养计算器年龄9 | 低于最小值 | 拒绝计算 |
| 无名称计划 | 空计划名 | 拦截保存 |
| 无动作计划 | 空动作列表 | 拦截保存 |

### 7.6 状态一致性（5 项）

| 场景 | 测试内容 | 预期结果 |
|------|----------|----------|
| 保存后草稿清空 | 保存训练后 | draft=[] |
| 保存后备注清空 | 保存训练后 | note='' |
| 保存后暂停重置 | 保存训练后 | sessionPaused=false |
| 保存后休息停止 | 保存训练后 | restRunning=false |
| 计划填充状态 | 填充计划后 | planInfo记录正确 |

### 7.7 并发操作（3 项）

| 场景 | 测试内容 | 预期结果 |
|------|----------|----------|
| 并发写入 | 20条训练+体重同时保存 | 数据完整 |
| 快速导入导出 | 3次快速导入 | 不重复 |

### 7.8 内存安全（2 项）

| 场景 | 测试内容 | 预期结果 |
|------|----------|----------|
| 大对象深拷贝 | 50动作×10组 | 不崩溃 |
| 超长备注 | 10KB备注 | 安全存储 |

### 7.9 搜索安全（7 项）

| 场景 | 测试内容 | 预期结果 |
|------|----------|----------|
| 正常搜索 | '卧推' | 返回结果 |
| 空搜索 | '' | 返回全部 |
| 脚本标签搜索 | '<script>' | 返回空数组 |
| 路径穿越搜索 | '../../' | 返回空数组 |
| 超长空格搜索 | 500个空格 | 返回空数组 |

## 8. 安全测试覆盖矩阵

### 7.1 XSS 与注入防护（7 项）

| # | 测试点 | 验证内容 |
|---|--------|----------|
| 1 | XSS 名称存储 | `<script>alert("xss")</script>` 作为 exerciseName 安全存储 |
| 2 | XSS 名称计算 | 含 XSS 字符的名称不影响容量计算 |
| 3 | 注入型备注 | `"><script>document.cookie</script><!-- ${7*7} -->` 原样保存 |
| 4 | 注入型计划名 | `<img onerror="alert(1)" src="x">` 原样保存 |

### 7.2 状态管理安全（3 项）

| # | 测试点 | 验证内容 |
|---|--------|----------|
| 1 | 训练页实例隔离 | 多个 train 页面实例状态互不影响 |
| 2 | 统计页只读 | loadStats() 不修改训练数据 |
| 3 | 食物页实例隔离 | 多个 food 页面实例状态互不影响 |

### 7.3 数据完整性（8 项）

| # | 测试点 | 验证内容 |
|---|--------|----------|
| 1 | 导入成功 | 合法数据导入成功 |
| 2 | 导出训练数一致 | 导出训练数量与导入一致 |
| 3 | 导出体重数一致 | 导出体重数量与导入一致 |
| 4 | 导出计划数一致 | 导出计划数量与导入一致 |
| 5 | 备注字段保留 | 导入导出往返备注不丢失 |
| 6 | RPE 字段保留 | 导入导出往返 RPE 不丢失 |
| 7 | 体重值保留 | 导入导出往返体重值精确 |
| 8 | 二次导入覆盖 | 重复导入不产生重复数据 |

### 7.4 并发安全（4 项）

| # | 测试点 | 验证内容 |
|---|--------|----------|
| 1 | 快速交替读写 | 50 次交替读写无数据丢失 |
| 2 | 同 id 快速覆盖 | 100 次同 id 覆盖不产生重复 |
| 3 | 覆盖后最新值 | 覆盖后数据为最后一次写入值 |
| 4 | 混合操作 | 保存/删除/查询交替操作数据一致 |

### 7.5 内存安全（5 项）

| # | 测试点 | 验证内容 |
|---|--------|----------|
| 1 | 大对象深拷贝 | 100 动作 ×20 组深拷贝安全 |
| 2 | 50KB 备注 | 超长备注安全存取 |
| 3 | 特殊字符全集 | `<>&"'\`{}[]()!@#$%^&*+=|\\/:;?~,.` 安全 |

### 7.6 边界值回归（16 项）

| # | 测试点 | 验证内容 |
|---|--------|----------|
| 1 | 0kg 自重 | 重量 0 视为自重动作 |
| 2 | 0.5kg 最小重量 | 最小重量精度 |
| 3 | 999.5kg 大重量 | 大重量不溢出 |
| 4 | -0 视为 0 | 负零等于零 |
| 5 | 0/1/999 次 | 次数边界值 |
| 6 | RPE 1/10/0/-1 | RPE 边界值 |
| 7 | 0 分钟消耗 | 0 分钟视为缺失，默认 45 分钟 |
| 8 | 负时长归 0 | 负时长不产生负卡路里 |
| 9 | 20kg/300kg 体重 | 体重边界值 |
| 10 | 0.1kg 精度 | 体重精度 |
| 11 | 营养计算器边界 | 最小/最大有效值、非法值拦截 |

### 7.7 数据迁移安全（6 项）

| # | 测试点 | 验证内容 |
|---|--------|----------|
| 1 | 脏数据迁移 | 含 null 元素的数组迁移不崩溃 |
| 2 | workouts 仍为数组 | 迁移后数据类型正确 |
| 3 | bodyweight 仍为数组 | 迁移后数据类型正确 |
| 4 | v2→v3 迁移 | 版本升级正确 |
| 5 | customPlans 初始化 | 新增字段正确初始化 |
| 6 | 迁移后导出正常 | 迁移后数据可正常导出 |

### 7.8 计算精度（7 项）

| # | 测试点 | 验证内容 |
|---|--------|----------|
| 1 | 浮点精度 | 62.5×3×2 = 375 |
| 2 | 大数精度 | 200×20 = 4000 |
| 3 | Epley 1RM | 100×10=133, 60×8=76, 50×5=58 |
| 4 | 容量汇总 | 两次 484 汇总 = 968 |

### 7.9 搜索安全（5 项）

| # | 测试点 | 验证内容 |
|---|--------|----------|
| 1 | 脚本标签搜索 | `<script>` 搜索安全 |
| 2 | 路径穿越搜索 | `../../` 搜索安全 |
| 3 | 空搜索 | 空字符串返回全部 |
| 4 | 超长空格搜索 | 1000 个空格搜索安全 |

### 7.10 食物数据安全（4 项）

| # | 测试点 | 验证内容 |
|---|--------|----------|
| 1 | 基础数据有效 | 食物字段完整热量为正 |
| 2 | 极端克数计算 | 99999g 热量计算有效 |
| 3 | 负克数处理 | 负克数产生负热量（UI 层拦截） |

---

## 8. 专项验证脚本清单

| 脚本 | 用途 | 项数 |
|------|------|------|
| `test.js` | 主测试套件（数据层 + 页面冒烟 + 发力图守门 + 工具函数） | 401 |
| `scripts/verify-boundaries.js` | 边界测试矩阵 | 45 |
| `scripts/verify-extreme.js` | 极限/压力/安全威胁 | 64 |
| `scripts/verify-hardening.js` | 高强度安全/容量/注入 | 61 |
| `scripts/verify-security-round2.js` | XSS/状态/完整性/并发/内存 | 73 |
| `scripts/verify-security-final.js` | 综合安全测试（存储/注入/防黑客/压力） | 147 |
| `scripts/verify-user-scenarios.js` | 用户场景测试（错误操作/中断/损坏恢复） | 68 |
| `scripts/verify-muscle-map.js` | 发力图↔部位卡片一致性 + 安全注入 | 43 |
| `scripts/verify-nav.js` | 导航审计 | 1 |
| `scripts/verify-page-match.js` | 页面数据匹配 | 4 |

**总计：907 项测试**
