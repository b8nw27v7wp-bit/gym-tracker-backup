<view class="page" wx:if="{{ex}}">
  <!-- 标题区 -->
  <view class="hero">
    <view class="hero-name">{{ex.name}}</view>
    <view class="hero-tags">
      <text class="hero-tag">{{ex.muscleName}}部</text>
      <text class="hero-tag">{{ex.typeText}}</text>
      <text class="hero-tag">{{ex.diffText}}</text>
      <text class="hero-tag">{{ex.equipText}}</text>
      <text class="hero-tag">{{ex.rest}}</text>
    </view>
  </view>

  <!-- 肌肉发力：文字描述 -->
  <view class="card">
    <view class="card-title">发力肌群</view>
    <view class="map-tags">
      <view class="map-tag-row" wx:if="{{ex.target.length > 0}}">
        <text class="map-tag-label">主要</text>
        <view class="map-tags-wrap">
          <text wx:for="{{ex.target}}" wx:for-item="t" wx:key="*this" class="map-chip map-chip-primary">{{t}}</text>
        </view>
      </view>
      <view class="map-tag-row" wx:if="{{ex.secondary.length > 0}}">
        <text class="map-tag-label">辅助</text>
        <view class="map-tags-wrap">
          <text wx:for="{{ex.secondary}}" wx:for-item="s" wx:key="*this" class="map-chip map-chip-secondary">{{s}}</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 动作要领 -->
  <view class="card">
    <view class="card-title">动作要领</view>
    <view wx:for="{{ex.steps}}" wx:key="*this" class="step-row">
      <text class="step-no">{{index + 1}}</text>
      <text class="step-text">{{item}}</text>
    </view>
  </view>

  <!-- 常见错误 -->
  <view class="card">
    <view class="card-title">常见错误</view>
    <view wx:for="{{ex.errors}}" wx:key="*this" class="err-row">
      <view class="err-dot"></view>
      <text class="err-text">{{item}}</text>
    </view>
  </view>

  <!-- 小贴士 -->
  <view class="card tip-card">
    <view class="tip-head">
      <view class="tip-dot"></view>
      <text class="tip-title">训练要点</text>
    </view>
    <text class="tip-text">{{ex.tip}}</text>
  </view>

  <!-- 部位训练知识 -->
  <view class="card" wx:if="{{muscle}}">
    <view class="card-title">{{muscle.name}}部训练知识</view>
    <view class="m-freq">{{muscle.freq}} · {{muscle.desc}}</view>
    <view wx:for="{{muscle.tips}}" wx:key="*this" class="m-tip-row">
      <text class="m-tip-dot">•</text>
      <text class="m-tip-text">{{item}}</text>
    </view>
  </view>

  <!-- 相关动作 -->
  <view class="card" wx:if="{{muscle.recommended.length > 0}}">
    <view class="card-title">同部位推荐</view>
    <view class="rel-row" wx:for="{{muscle.recommended}}" wx:key="id" data-id="{{item.id}}" bindtap="onRelatedTap">
      <text class="rel-name">{{item.name}}</text>
      <text class="rel-arrow">›</text>
    </view>
  </view>

  <!-- 关联阅读 -->
  <view class="card" wx:if="{{articles.length > 0}}">
    <view class="card-title">关联阅读</view>
    <view class="rel-row" wx:for="{{articles}}" wx:key="id" data-id="{{item.id}}" bindtap="onArticleTap">
      <view class="rel-left">
        <text class="rel-name">{{item.title}}</text>
        <text class="rel-summary">{{item.catName}} · {{item.summary}}</text>
      </view>
      <text class="rel-arrow">›</text>
    </view>
  </view>

  <!-- 底部操作 -->
  <view class="footer">
    <view class="save-btn" bindtap="onRecord">去记录这个动作</view>
  </view>
</view>
